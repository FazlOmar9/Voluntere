# ./docker-compose.yml
version: '3'
services:
  backend:
    build: ./backend
    ports:
      - 3010:3010
  next:
    build: ./next
  nginx:
    build: ./nginx
    ports:
      - 80:80
  mongodb:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - ./data/db:/data/db